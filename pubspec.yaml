name: encontrapet
description: "app-encontra-pet android"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  provider: ^6.1.5+1
  sqflite: ^2.4.2+1
  path_provider: ^2.1.5
  path: ^1.9.1
  supabase_flutter: ^2.12.4
  flutter_dotenv: ^6.0.1
  connectivity_plus: ^7.1.1
  uuid: ^4.5.3
  image_picker: ^1.1.2
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  easy_mask: ^2.0.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:


  uses-material-design: true

  assets:
    - .env
